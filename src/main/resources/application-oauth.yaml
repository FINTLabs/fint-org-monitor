fint:
  security:
    oauth2:
      clients:
        fint-core:
          token-uri: https://idp.felleskomponent.no/nidp/oauth/nam/token
          client-id: ${fint.core.oauth2.client-id}
          client-secret: ${fint.core.oauth2.client-secret}
          authorization-grant-type: password
          username: ${fint.core.oauth2.username}
          password: ${fint.core.oauth2.password}
          scope:
            - fint-client
  core:
    idp:
      url: https://idp.felleskomponent.no/nidp/oauth/nam/token
      oauth-client: fint-core

# resource-owner-password flow

# sjekk idpconfig for hvordan det brukes i flais-idp-gateway
# kopier over denne mappa: https://github.com/FINTLabs/flais-idp-gateway/tree/main/src/main/kotlin/no/fintlabs/flais/security/oauth2/client
